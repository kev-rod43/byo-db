import {
    CREATE_COLLECTION,
    DELETE_COLLECTION,
    UPDATE_COLLECTION,
    CREATE_PRODUCT,
    DELETE_PRODUCT,
    UPDATE_PRODUCT,
    CREATE_TAG,
    DELETE_TAG,
    UPDATE_TAG,
    APPLY_TAG,
    UNAPPLY_TAG,
    SORT_PRODUCT
} from './actions';

export default function reducer(state, action) {
    switch (action.type){
        case CREATE_COLLECTION :{
            
        }
    }
}